if(!multidirectional || 1 == 1){
	
	coll.x += (sign(image_xscale) / 2) + 64;
	
	//coll.image_xscale = (sprite_width) / -16;
	//coll.image_yscale = sprite_height / 16;	
	log( string(coll.image_xscale) + "is the door's xscale")
	log( string(coll.image_yscale) + "is the door's yscale")
}